定义：
指针就是地址，地址就是指针。
指针变量是存放地址的变量（上面是普通变量，下面是指针变量）

int * p;  //int *表示整形指针，变量名是p；
int i = 3;
p=&i; //i的地址
*p = 5; //*p等价于i

指针可以在函数中改变主函数的值，因为通过地址可以找到变量。
当然还有很多，比如数组和指针的关系，字符串，FILE，多级指针，函数指针等等，当时没记，现在也懒得写了，鸽着吧。
